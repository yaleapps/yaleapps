import { QBtn, QTab } from "quasar";
import { setQuasarComponentDefaultPropValues } from "src/utils/quasar.utils";

setQuasarComponentDefaultPropValues(QBtn, { noCaps: true });
setQuasarComponentDefaultPropValues(QTab, { noCaps: true });
